// DESAFIO CON CONDICIONALES

/**
 * Crear un prompt que reciba un número y que tenga el texto:
 * "Ingrese el stock del producto:", guardar ese número en una variable
 * llamada stock.
 */

/**
 * Crear un condicional que imprima por consola "El producto tiene stock" si
 * el stock es mayor que 0 (cero) y "El producto no tiene stock" si es igual
 * a 0 (cero).
 */

